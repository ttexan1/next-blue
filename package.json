{
  "name": "next-blue",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx ts-node src/server.ts",
    "build": "NODE_ENV=production next build && tsc --project tsconfig.server.json",
    "gcp-build": "NODE_ENV=production next build && tsc --project tsconfig.server.json",
    "start": "NODE_ENV=production node build/server.js",
    "deploy": "gcloud app deploy"
  },
  "dependencies": {
    "next": "10.0.2",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.9",
    "@types/react": "^17.0.0",
    "typescript": "^4.1.2"
  }
}
